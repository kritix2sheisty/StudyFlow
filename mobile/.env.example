# Where the StudyFlow API is. Copy this file to .env and set your laptop's LAN IP
# (Windows: ipconfig -> IPv4 Address). The phone must be on the same Wi-Fi.
# No trailing slash. After editing .env restart with: npx expo start -c
EXPO_PUBLIC_API_URL=http://192.168.100.24:8010
